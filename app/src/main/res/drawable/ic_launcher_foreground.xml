<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        Standard adaptive icon safe zone is 66dp or 72dp inside 108dp.
        We use 72dp to maximize visibility but avoid clipping.
    -->
    <item
        android:width="72dp"
        android:height="72dp"
        android:drawable="@drawable/logo"
        android:gravity="center" />
</layer-list>
